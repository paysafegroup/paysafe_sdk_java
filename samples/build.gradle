apply plugin: "war"

repositories {
    mavenCentral()
}

dependencies {
  implementation project(":PaysafeSDK")
  implementation group: 'com.google.code.gson', name: 'gson', version: '2.2.2+'
  implementation "javax.servlet:servlet-api:2.5"
}
